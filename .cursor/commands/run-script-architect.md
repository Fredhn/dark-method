Run the DARK-METHOD **Script Architect Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/script-architect.agent.md`. Read the approved Production Brief from `projects/{currentProjectFolder}/production-brief.md`; write the narration script to `projects/{currentProjectFolder}/script.md`. Present the script and run the approval gate. After approval, tell the user: **To run the next agent, type /run-retention-editor in chat or select it from the slash menu (/)**.
