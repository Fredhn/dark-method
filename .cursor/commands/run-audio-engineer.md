Run the DARK-METHOD **Audio Engineer Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/audio-engineer.agent.md`. Read the approved edit structure from `projects/{currentProjectFolder}/visuals.md`; write the audio mix guide to `projects/{currentProjectFolder}/audio.md`. Include Manual Step for audio mixing. Present the artifact and run the approval gate. After approval, tell the user: **To run the next agent, type /run-packaging in chat or select it from the slash menu (/)**.
