Run the DARK-METHOD **Editor Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/editor.agent.md`. Read the approved visual blueprint from `projects/{currentProjectFolder}/visuals.md`; write the editing playbook to `projects/{currentProjectFolder}/visuals.md`. Include Manual Step for timeline assembly. Present the artifact and run the approval gate. After approval, tell the user: **To run the next agent, type /run-audio-engineer in chat or select it from the slash menu (/)**.
