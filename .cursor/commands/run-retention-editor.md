Run the DARK-METHOD **Retention Editor Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/retention-editor.agent.md`. Read the approved script from `projects/{currentProjectFolder}/script.md`; write the retention-optimized script to `projects/{currentProjectFolder}/script.md`. Present the script and run the approval gate. After approval, tell the user: **To run the next agent, type /run-voice-director in chat or select it from the slash menu (/)**.
