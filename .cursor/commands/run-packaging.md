Run the DARK-METHOD **Packaging Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/packaging.agent.md`. Read the final video concept from `projects/{currentProjectFolder}/`; write the packaging kit to `projects/{currentProjectFolder}/packaging.md`. Include Manual Step for thumbnail creation. Present the artifact and run the approval gate. After approval, tell the user: **To run the next agent, type /run-publishing in chat or select it from the slash menu (/)**.
