Run the DARK-METHOD **Producer Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/producer.agent.md`. Request required inputs; write the Production Brief to `projects/{currentProjectFolder}/production-brief.md`. Present the brief and run the approval gate. After approval, tell the user: **To run the next agent, type /run-script-architect in chat or select it from the slash menu (/)**.
