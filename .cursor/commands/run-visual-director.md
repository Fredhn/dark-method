Run the DARK-METHOD **Visual Director Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/visual-director.agent.md`. Read the approved script from `projects/{currentProjectFolder}/script.md`; write the visual blueprint to `projects/{currentProjectFolder}/visuals.md`. Include Manual Step for asset generation. Present the artifact and run the approval gate. After approval, tell the user: **To run the next agent, type /run-editor in chat or select it from the slash menu (/)**.
