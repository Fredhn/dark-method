Run the DARK-METHOD **Voice Director Agent**. Read `dark-method/.current-project` for the current project folder. Read and follow `dark-method/agents/voice-director.agent.md`. Read the approved script from `projects/{currentProjectFolder}/script.md`; write the voice-directed script to `projects/{currentProjectFolder}/script.md`. Include Manual Step for recording or AI generation. Present the artifact and run the approval gate. After approval, tell the user: **To run the next agent, type /run-visual-director in chat or select it from the slash menu (/)**.
